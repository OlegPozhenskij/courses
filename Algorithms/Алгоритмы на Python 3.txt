Образовательные курсы, которые я прохожу, или уже прошёл

2017-2018 Алгоритмы и структуры данных на Python 3  (МФТИ)
-
https://youtube.com/playlist?list=PLRDzFCPr95fK7tr47883DFUbm4GeOjjc0

Дата просмотра лекций == дата коммитов

Лекция №1:
	Были изучены:
		* пременная - имя, ссылающееся на зарезервированный объект в ячейке памяти 
		* существуют изменяемые и неизменяемые объекты
		* изменяемые передаются по ссылке, неизменяемые по объекту
		* принцип работы операции a, b = b, a
		* мат. принцип (-12)//5 и -12 % 5
		* while(): else: (else = finally)
		* for x in range(start, stop, step) -> math [start;stop)

Лекция №2:
	Были изучены:
		* принципы алгебры логики 
		* вложенные последовательные if'ы
		* else не имеет возможности указывать условаие
		* elif - как решение по избежанию кучи отступов (линеаризации)

Лекция №3:
	Были изучены:
		* цифра - символ для кодирования числа
		* системы счисления и переводы между ними
		* однопроходные алгоритмы 

Лекция №4:
	Были изучены:
		* функции
		* duck typing	
		* принцип работы call stack'а
		* Алгоритм brute force - последовательный перебор возможных вариантов до достижения нужного нам
		* Алгоритм Факторизации - разложение числа на простые множители

Лекция №5:
	Были изучены:
		* массивы A = [0] * 1000
		* Алгоритм копирование элементов массива
		* циклический сдвиг влево
		* перестановка элементов наоборот 

Лекция №6:
	Были изучены:
		* массивы A = [0] * 1000
		* list compricative
		* квадратические сортировки - O(n^2)		
		* insert sort
		* choise sort
		* buble sort
		* сортировка подсчётом O(n)
